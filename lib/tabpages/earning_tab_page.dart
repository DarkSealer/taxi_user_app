import 'package:flutter/material.dart';

class EarningTabPage extends StatelessWidget {
  EarningTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is the earning tab page"),
    );
  }
}
